# RSA-and-Steganography
1. PyCryptodome is used to implement RSA encryption algorithm.
2. The encrypted message is then hidden behind an image.
3. The original message can be decrypted from the image.
